# Unified backend API (not the direct WAHA port)
WAHA_API_URL=http://localhost:8200/api

# Required unified-backend credential. Never commit a real value.
WAHA_API_KEY=replace-with-backend-api-key

# Optional WAHA session and outbound-send throttle
WAHA_SESSION=default
WAHA_SEND_DELAY_MS=1000
